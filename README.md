# ID Scanner with OCR

A simple **ID card scanner** built with **Streamlit** and **Tesseract OCR** to extract text information from uploaded ID card images.

---

## 📝 Features

- Upload ID card images (`.jpg`, `.jpeg`, `.png`)
- Extract text information using **OCR (Optical Character Recognition)**
- Display extracted details like:
  - Organization Name
  - Employee Name
  - Phone number
  - Other ID info
- Works with real ID card images

---

## 🚀 Demo

Open the Streamlit app:

```bash
streamlit run app.py
